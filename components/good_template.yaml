component:
  # Имя темплейта, которое будет указано в запросе
  name: "my_component"

  template:
    
    # Задаем настройки - маленькие информационные блоки
    settings:
      - std_setup
      - filebeat_mappings_setup
      - filebeat_query_setup

    aliases:
      some_alias: